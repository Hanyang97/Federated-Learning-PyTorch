# python=3.7.3
# pytorch=1.2.0
# torchvision=0.4.0
# numpy=1.15.4
# tensorboardx=1.4
# matplotlib=3.0.1
import matplotlib as plt
import torch
import torchvision
import numpy as np
import tensorboardX 

print('matplotlib version is {}'.format(plt.__version__))
print('torchvision version is {}'.format(torchvision.__version__))
print('numpy version is {}'.format(np.__version__))
print('tensorboardX version is {}'.format(tensorboardX.__version__))
print('torch version is {}'.format(torch.__version__))